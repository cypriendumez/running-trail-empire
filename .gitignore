# Dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# Testing
/coverage

# Next.js
# ⚠️ iCloud duplique les dossiers en « .next 2 », « .next 3 »… que « /.next/ »
# ne couvre PAS : 2 106 fichiers de build s'étaient ainsi retrouvés commités.
/.next*
/out/

# Production
/build

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env*.local
.env

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Supabase
/supabase/.branches
/supabase/.temp
handoff-parcours/

# Logs
*.log

# Captures d'écran / archives locales
Capture*.png
*.zip

# Worktrees Claude
/.claude/worktrees/

# Dossier data/ : on ne versionne QUE les JSON lus par l'app (/api/parcours).
# Le reste (artefacts de crawl, caches, jsonl) est régénérable / hors-app.
/data/*
!/data/parcours_certifies.json
!/data/dataset_france.json
!/data/dataset.json
.verif/

# iCloud restaure ce fichier tout seul : Strava a été retiré de la rangée le 21/08/2026
# (c' est un journal d'activités, pas une montre). L'ignorer empêche un `git add -A`
# de le remettre en ligne à notre insu.
public/brands/strava.svg
# Polar retiré de la vitrine le 23/08/2026 : son logo est une marque déposée, il n'a
# plus rien à faire en production. Ignoré car iCloud restaure ce dossier tout seul.
public/brands/polar.svg

# fichiers de travail jetables (audits ponctuels en base)
/.scratch/
